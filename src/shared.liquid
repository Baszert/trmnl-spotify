{% assign top_tracks_raw = IDX_0 %}
{% assign top_artists_raw = IDX_1 %}
{% assign recent_raw = IDX_2 %}
{% assign screen = trmnl.plugin_settings.custom_fields_values.view %}

<style>
  /* Scoped to #trmnl-spotify so these don't leak into other plugins in a mashup */
  #trmnl-spotify .relative {
    position: relative;
  }

  #trmnl-spotify .absolute {
    position: absolute;
  }

  #trmnl-spotify .bold {
    font-weight: 800;
  }

  #trmnl-spotify .rank {
    top: 5px;
    left: 5px;
  }
</style>